steps:
- name: 'gcr.io/cloud-builders/docker'
  id: 'Build container image'
  args: [
    'build',
    '.',
    '-t',
    'europe-west1-docker.pkg.dev/eighth-saga-484310-f8/wikiart-registry/$_IMAGE_NAME',
    '-f',
    './dockerfiles/$_DOCKER_NAME.dockerfile'
  ]

- name: 'gcr.io/cloud-builders/docker'
  id: 'Push container image'
  args: [
    'push',
    'europe-west1-docker.pkg.dev/eighth-saga-484310-f8/wikiart-registry/$_IMAGE_NAME'
  ]

options:
  logging: CLOUD_LOGGING_ONLY
substitutions:
  _IMAGE_NAME: 'my_image'
  _DOCKER_NAME: 'train'
